# Python
.venv/
__pycache__/
*.pyc

# Generated output / scratch
output/
scratchpad_*
sales_credit_report_*.xlsx

# Employee-submitted quotations + attachments (real business data — keep out of Git)
data/quotations.csv
data/attachments/
data/completions.csv
data/notifications.csv
data/users.csv
data/billing_status.csv
data/sales_overrides.csv
data/quotation_requests.csv
data/quotation_request_status.csv

# Client PDFs / documents (e.g. quotation references) — keep out of Git
*.pdf

# Streamlit local secrets (NEVER commit) — config.toml is fine to commit
.streamlit/secrets.toml

# OS
Thumbs.db
.DS_Store

# Client workbook stays LOCAL ONLY (repo is public — never commit client data).
Sample file/
